package jwt_test

import "testing"

func TestGenerate(t *testing.T) {
	tt := []struct {
		name string
	}{
		{},
	}

	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}

func TestParse(t *testing.T) {
	tt := []struct {
		name string
	}{
		{},
	}

	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}
