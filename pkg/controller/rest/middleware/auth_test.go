package middleware_test

import "testing"

func TestAuthorizationRequest(t *testing.T) {
	tt := []struct {
		name string
	}{
		{},
	}

	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}
